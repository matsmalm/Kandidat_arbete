void preGenetic(); /* Arguments are Node Matrix, {Pursuers, start paths}, BREAK. BREAK is how many generations should be run before termination. */
void genAlg(); /* Arguments are Node Matrix, {Pursuers, start paths}, BREAK. BREAK is how many generations should be run before termination. */
